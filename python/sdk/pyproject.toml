[project]
name = "cmsx-autograder"
version = "0.1.0"
description = "Python SDK for CMSX autograder grading scripts"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
